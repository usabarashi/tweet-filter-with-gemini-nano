\* TLC Model Configuration for TweetFilter
\* Memory analysis configuration

CONSTANTS
    TweetIds = {t1, t2}
    MaxCacheSize = 2
    MaxQueueLen = 2
    MaxInFlight = 2
    MaxMemoryBudget = 20

INIT Init

NEXT Next

CONSTRAINT StateConstraint

INVARIANTS
    TypeOK
    CacheBounded
    EvalQueueBounded
    SessionTypeConsistency
    ClonedSessionsBounded
    PipelineDepthBounded
    \* --- Memory analysis invariants (VIOLATED) ---
    \* NoOrphanedMessagesOnDisable: VIOLATED
    \*   Trace: DiscoverTweet -> SendEvaluateRequest -> ConfigChange(disabled)
    \*   Issue: EvaluateRequest remains in contentToBg after filtering disabled
    \*   Code: Content.Main.applyTransition does not drain in-flight messages
    \*
    \* BgFibersBounded: VIOLATED
    \*   Trace: SendInitRequest -> BgHandle -> SendInitRequest -> BgHandle -> ...
    \*   Issue: Unlimited InitRequests create unbounded Aff fibers in Background
    \*   Code: Content.Main.ensureInitializedAndEnabledAsync uses launchAff_
    \*         without guarding against concurrent calls
    \*
    \* ChannelsBounded: VIOLATED
    \*   Trace: ConfigChange -> Reinit -> ConfigChange -> Reinit -> ConfigChange -> Reinit
    \*   Issue: Rapid config changes accumulate responses in offscreenToBg
    \*   Code: Background.Main.onConfigChange uses launchAff_ without
    \*         waiting for previous reinit to complete
    \*
    \* MemoryBudgetRespected: VIOLATED (21 > 20 at depth 14)
    \*   Breakdown: contentQueue DOM refs (9) + bgFibers (6) + clonedSession (5) + messages (1)
    \*   Issue: Combined pressure from DOM element retention + fiber accumulation + AI session
    \*
    \* --- Invariants passing (verified over 314M+ states) ---
    \*   TypeOK, CacheBounded, EvalQueueBounded, SessionTypeConsistency,
    \*   ClonedSessionsBounded, PipelineDepthBounded
